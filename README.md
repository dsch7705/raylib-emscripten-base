# Base for raylib web applications
# Notes
- Be sure Emscripten environment variables are set in the current context (```source ./emsdk_env.sh```)
- To start localhost: ```emrun app.html``` in build directory